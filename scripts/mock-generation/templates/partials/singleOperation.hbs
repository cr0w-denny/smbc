    const entityId = params.{{pathParam}} as string;
    const item = {{lowerEntityName}}DataStore.get(entityId);
    
    if (!item) {
      return HttpResponse.json(
        { error: 'Not found', message: '{{entityName}} not found' },
        { status: 404 }
      );
    }

    return HttpResponse.json(item);