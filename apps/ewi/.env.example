# EWI App Environment Variables

# Enable mock service worker (MSW) for API mocking
# Set to "true" to force mocks in production
# Set to "false" to disable mocks in development
# Default: true in development, false in production
VITE_USE_MOCKS=true

# API base URL (when not using mocks)
# Example: https://api.example.com
VITE_API_BASE_URL=

# Base path for the application
# This is automatically set in GitHub Pages deployment
# VITE_BASE_PATH=/smbc/ewi/