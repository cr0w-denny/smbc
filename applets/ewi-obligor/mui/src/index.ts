import { Applet } from "./Applet";
import permissions from "./permissions";

export default {
  permissions,
  component: Applet,
  apiSpec: {
    name: "EWI Obligor API",
    spec: null, // No API spec for now
  },
};