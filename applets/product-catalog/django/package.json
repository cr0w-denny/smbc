{
  "name": "@smbc/product-catalog-django",
  "version": "1.1.0",
  "description": "Django backend implementation for product catalog",
  "private": true,
  "scripts": {
    "serve": "python manage.py runserver",
    "build": "echo 'Django deployment preparation'",
    "test": "exit 0 && python manage.py test"
  }
}
