/**
 * MUI-specific filter types that extend core types
 */

import type { FilterFieldConfig, FilterValues, FilterSpec, FilterProps } from "@smbc/ui-core";

// Re-export core types for convenience
export type { FilterFieldConfig, FilterValues, FilterSpec, FilterProps };
